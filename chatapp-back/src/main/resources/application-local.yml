logging:
  level:
    ROOT: INFO
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
    com.example.chatapp_back: DEBUG
spring:
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true